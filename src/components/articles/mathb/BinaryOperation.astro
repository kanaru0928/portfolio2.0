---
import InlineKatex from "@components/libs/InlineKatex";
import Model from "./Model.astro";

interface Props {
  symbol: string;
  className?: string;
}

const { symbol, className } = Astro.props;
---

<Model symbol={symbol} title="二項演算 (Binary Operation)" className={className}>
  <slot name="header" slot="header" />
  <slot />
</Model>
